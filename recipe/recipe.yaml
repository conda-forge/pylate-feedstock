context:
  version: "1.2.0"

package:
  name: pylate
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/p/pylate/pylate-${{ version }}.tar.gz
    sha256: f56af648da961f4add7b1fdc2abdeb50f48876bcab934a2a9fbc8f29972bcc2d

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install .

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - sentence-transformers ==3.4.1
    - datasets >=2.20.0
    - accelerate >=0.31.0
    - voyager >=2.0.9
    - sqlitedict >=2.1.0
    - pandas >=2.2.1
    - transformers ==4.48.2
tests:
  - python:
      imports:
        - pylate
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/lightonai/pylate
  license: MIT
  summary: A library for training and retrieval with ColBERT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - moritzwilksch
